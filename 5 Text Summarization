# -*- coding: utf-8 -*-
"""DSPA5.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1mwpaP09ER81xnKp8RMKJj4miSlezKVMj
"""

# Practical No : 05
# Aim: Text Summarization

from transformers import pipeline

def summarize_text(text, max_length=150, min_length=50):
    # Load the BART model for summarization
    summarizer = pipeline("summarization", model="facebook/bart-large-cnn")
    # Perform summarization
    summary = summarizer(text, max_length=max_length, min_length=min_length, do_sample=False)

    return summary[0]['summary_text']

# Example usage
long_text = """Artificial Intelligence (AI) is revolutionizing various industries
by enabling machines to perform tasks that traditionally required human intelligence. From automating repetitive processes in manufacturing to enhancing decision-making in business,AI is transforming how we interact with technology.In healthcare, AI is helping doctors diagnose diseases more
accurately and develop personalized treatment plans for patients.
In the automotive industry, self-driving cars powered by AI are paving
the way for safer and more efficient transportation.
However, while AI brings significant benefits,
it also raises concerns about job displacement and ethical issues,
such as privacy and bias in algorithms. As AI continues to evolve,
it is essential to develop frameworks to ensure its responsible and
fair use across all sectors."""

# Summarize the long text
summary = summarize_text(long_text)

# Print the results
print("Original text length:", len(long_text))
print("Summary length:", len(summary))
print("\nSummary:")
print("\n".join(summary.split(". ")))
