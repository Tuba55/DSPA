# -*- coding: utf-8 -*-
"""DSPA3.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1jQGt7paJi52sqLvW9tmcP1SQUozt3iEu
"""

# Install required libraries
!pip install streamlit
!pip install transformers torch
!pip install requests

# Get Public IP Address
!wget -q -O - ipv4.icanhazip.com

# Run Streamlit app using localtunnel
!streamlit run app.py & npx localtunnel --port 8501

# Content of app.py
'''
app.py
import streamlit as st
from transformers import pipeline

# Load Translation Model
@st.cache_resource
def load_model():
    return pipeline("translation_en_to_hi", model="Helsinki-NLP/opus-mt-en-hi")

# Load the model
translator = load_model()

# Streamlit UI
st.title("🌍 English to Hindi Translator")

# User input
text = st.text_area("Enter text in English:")

# Button to translate
if st.button("Translate to Hindi"):
    if text.strip():
        translation = translator(text)
        st.subheader("🔹 Translated Text:")
        st.write(translation[0]['translation_text'])
    else:
        st.warning("⚠️ Please enter some text.")
'''
